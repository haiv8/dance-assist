# 文件讲解清单（演示用）

## 1. 一键启动
- `run-all.bat`：同时启动后端和前端。
- `run-backend.bat`：只启动后端。
- `run-frontend.bat`：只启动前端。

## 2. 前端（给老师看的页面）
- `frontend/src/main.ts`：前端入口，挂载 Vue 应用。
- `frontend/src/router/index.ts`：页面路由（上传页、对比页）。
- `frontend/src/pages/UploadPage.vue`：视频上传页面。
- `frontend/src/pages/ComparePage.vue`：分析与结果展示页面（核心演示页）。
- `frontend/src/api/http.ts`：前端请求基础配置。
- `frontend/src/api/videos.ts`：视频上传与列表接口调用。
- `frontend/src/api/pipelines.ts`：启动分析、查状态、拿结果接口调用。
- `frontend/src/types/video.ts`：前后端数据结构类型定义。

## 3. 后端（接口层）
- `backend/app/main.py`：FastAPI 启动入口，注册全部路由。
- `backend/app/settings.py`：后端配置（路径、跨域、接口前缀等）。
- `backend/app/api/videos.py`：视频上传与视频列表接口。
- `backend/app/api/pipelines.py`：分析流程启动、状态查询、结果查询接口。
- `backend/app/api/media.py`：视频/产物文件流式访问接口（支持 Range 分段播放）。
- `backend/app/schemas/dto.py`：接口请求和响应的数据模型。

## 4. 后端（服务层）
- `backend/app/services/storage.py`：保存上传文件、视频标准化转码、元数据管理。
- `backend/app/services/pipeline.py`：后台线程运行算法流水线并组装返回结果。

## 5. 核心算法（技术重点）
- `core/pose_mediapipe.py`：MediaPipe 姿态关键点提取（33 关键点）。
- `core/pipeline.py`：核心流程：特征构建、DTW 对齐、评分、报告生成。
- `core/align_and_score.py`：单视频对的对齐评分封装。
- `core/timeline.py`：时间轴数据生成（给前端同步播放用）。
- `core/render_overlay.py`：骨架叠加视频渲染并转码为 H.264。
- `core/config.py`：算法参数配置定义。
- `core/types.py`：算法层数据结构定义。

## 6. 数据与产物目录（演示时可顺带说明）
- `backend/uploads/`：上传后的教师/用户视频及元数据。
- `data/standard_videos`：标准教师视频。
- `data/user_videos`：用户视频。
- `data/cache_keypoints`：关键点缓存（npy）。
- `outputs/<pair_name>/`：每次分析的结果产物（report/timeline/图像/叠加视频）。

## 7. 你明天现场讲解顺序（推荐）
1. 先双击 `run-all.bat` 启动服务。  
2. 打开 `UploadPage.vue` 讲“上传与数据入口”。  
3. 打开 `ComparePage.vue` 讲“分析和可视化主页面”。  
4. 打开 `backend/app/api/pipelines.py` 讲“如何触发算法任务”。  
5. 打开 `backend/app/services/pipeline.py` 讲“后台如何跑流程”。  
6. 打开 `core/pipeline.py` 讲“算法主线（MediaPipe + DTW + 评分）”。  
7. 最后展示 `outputs/` 里的 `report.json` 与叠加视频。  

## 8. 一句话版（紧张时可直接说）
“这个项目是前后端分离，前端负责上传和展示，后端负责接口和任务调度，核心算法用 MediaPipe 提取姿态点，再用 DTW 做时序对齐，最后输出评分、问题定位和可视化结果。”
