# dance-assist 项目架构升级记录（2026-03-11）

## 1. 本次升级目标

本轮升级的目标不是改算法效果，而是在**不破坏现有演示功能**的前提下，把 `dance-assist` 从“本地可跑的课程项目”逐步升级为“更接近可上线、可持续迭代的工程化项目”。

本次升级重点放在两类问题上：

1. 工程化底座不稳：任务状态只在内存里、重启后丢失，接口返回过重，配置分散，不利于后续扩展。
2. 运行产物过大：算法运行后生成的大文件直接堆在项目目录中，导致整个项目体积膨胀。

## 2. 今天已经完成的升级项

### 2.1 运行数据外置与项目瘦身

已完成内容：

- 运行目录从仓库根目录迁移为 `DANCE_ASSIST_HOME` 外置目录。
- 上传文件、缓存、模型、输出结果统一迁移到运行目录管理。
- 增加输出保留策略，避免分析结果无限累积。
- 删除超大 `outputs/*_outputs.zip` 运行产物。
- 增加项目体积审计和清理脚本。
- 增加桌面启动入口和 EXE 打包脚本。

直接收益：

- `outputs` 目录从约 `44.78GB` 降到约 `78MB` 量级。
- 仓库目录不再天然承载运行数据，后续双击运行或打包分发更安全。
- 项目源码、运行数据、构建产物三者的边界更清晰。

### 2.2 配置中心化

已完成内容：

- 后端配置改为优先读取项目根目录 `.env`。
- 新增 `.env.example` 作为标准配置模板。
- 收敛了 API 前缀、日志级别、上传限制、数据库连接、执行器类型、Redis 配置、运行根目录等配置项。

直接收益：

- 原来偏“写死在代码里”的运行参数，改成了“环境驱动”。
- 后续切换本地环境、桌面环境、部署环境时，不需要频繁改源码。

### 2.3 API 版本化与结果分层

已完成内容：

- 保留旧接口 `/api/*`。
- 新增版本化接口 `/api/v1/*`。
- 增加 `result/summary` 和 `result/frame/{frame}` 分层结果接口。
- 前端对比页默认优先拉取摘要结果，帧级详情按需加载。

直接收益：

- 旧页面不需要大改，兼容性保留。
- 全量 `frame_analysis` 不再默认一次性返回，接口更轻，页面更稳。
- 后续前端升级可以逐步迁移到 `/api/v1`，不容易互相牵制。

### 2.4 请求日志与基础可观测性

已完成内容：

- 为请求增加 `request_id`。
- 记录请求方法、路径、状态码、耗时。
- 健康检查接口增加当前任务存储后端和执行器后端信息。

直接收益：

- 出现问题时，可以更容易定位哪次请求、哪条链路出了问题。
- 为后续指标统计、错误归因、告警补齐打了基础。

### 2.5 PostgreSQL 任务与元数据持久化

已完成内容：

- 本地 PostgreSQL 已搭建并跑通。
- 数据库连接已经接入项目 `.env`。
- 已建表：
  - `video_records`
  - `pipeline_tasks`
  - `pipeline_task_events`
- 视频元数据已支持写入 PostgreSQL。
- 任务状态已支持写入 PostgreSQL。
- 任务事件已支持写入 PostgreSQL。
- 仍保留 JSON/文件落盘兜底，避免数据库异常时项目直接不可用。

当前已验证状态：

- `video_records = 2`
- `pipeline_tasks = 4`
- `pipeline_task_events = 9`

直接收益：

- 升级前：任务状态只在内存字典里，服务重启后容易丢失。
- 升级后：任务和事件已经具备持久化能力，可用于追踪、看板、统计、恢复。

### 2.6 执行器抽象：从“直接起线程”升级为“可替换执行后端”

已完成内容：

- 将 `pipeline.py` 中直接启动线程的方式抽象为执行器接口。
- 当前支持两类执行器：
  - `local_thread`
  - `redis_queue`
- 健康检查接口可返回当前执行器类型。

直接收益：

- 升级前：业务层与执行方式耦合，后续要接队列会改动很大。
- 升级后：业务层只负责提交任务，执行后端可以切换，后续接 Worker 会平滑很多。

### 2.7 Redis 队列 Worker 骨架与本地联调

已完成内容：

- 增加 Redis 队列执行器。
- 增加 Redis Worker 入口。
- 增加 Redis Worker 启停/状态脚本。
- 本地 `.env` 已切换为：`DANCE_ASSIST_PIPELINE_EXECUTOR=redis_queue`。
- 本地 Redis 地址使用：`redis://127.0.0.1:6379/0`。

当前已验证结果：

- 最新任务 `74e94d07e73c4925` 已通过 `redis_queue` 执行成功。
- PostgreSQL 中该任务的事件链完整：
  - `queued`
  - `started`
  - `completed`
- 说明“API 提交任务 -> Redis 入队 -> Worker 消费 -> PostgreSQL 落状态/事件 -> 算法产出结果”这条链路已经打通。

## 3. 升级前后对比

| 对比项 | 升级前 | 升级后 |
|---|---|---|
| 运行数据位置 | 直接写在项目目录内 | 统一迁移到 `DANCE_ASSIST_HOME` |
| 项目体积 | 易被输出产物迅速撑大 | 输出保留策略 + 外置运行目录，体积可控 |
| 任务状态 | 内存字典 + 本地线程 | PostgreSQL 持久化 + 执行器抽象 |
| 抗重启能力 | 服务重启后状态容易丢 | 状态和事件已可从存储恢复 |
| 接口结构 | `/api` 单版本 | `/api` 兼容保留，新增 `/api/v1` |
| 结果返回 | 默认整份大结果返回 | 摘要/帧级详情分层返回 |
| 前端加载 | 对大结果更敏感 | 默认轻量加载，按需拉细节 |
| 任务执行方式 | 业务逻辑直接起线程 | `local_thread / redis_queue` 可切换 |
| 可观测性 | 基本没有链路信息 | 有 `request_id`、耗时、状态、任务事件 |
| 数据库能力 | 无真正业务数据库承载 | PostgreSQL 已承接视频、任务、事件 |
| 桌面交付准备 | 偏源码工程 | 已补桌面启动器和 EXE 打包脚本 |

## 4. 本次升级后，当前项目已经具备的能力

当前项目已经不是单纯的“跑通算法 demo”，而是具备了以下工程化能力：

1. 有可配置的运行环境。
2. 有外置运行目录，不再强依赖仓库目录。
3. 有数据库持久化任务与元数据。
4. 有请求级日志和任务事件链。
5. 有版本化 API 和分层结果接口。
6. 有可替换执行器，支持本地线程和 Redis 队列。
7. 有桌面启动和打包方向的基础脚手架。

## 5. 当前仍然存在的差距

虽然今天的升级已经把项目从“能演示”推进到了“更像一个工程项目”，但距离真正的“上线级成熟系统”还有几个明显差距：

### 5.1 数据层仍是“混合模式”

目前视频、任务、事件已经接入 PostgreSQL，但文件本体仍以本地文件形式存在，尚未切换到对象存储。

后续应升级为：

- PostgreSQL：存元数据、任务、报告索引、用户信息
- 对象存储：存视频、报告附件、叠加视频、图像产物

### 5.2 权限体系尚未建立

当前系统仍偏单机演示使用方式，尚未实现：

- 登录鉴权
- 用户角色（学员/教师/管理员）
- 上传额度限制与风控
- 操作审计

### 5.3 任务调度仍缺少生产级能力

目前 Redis 队列链路已打通，但还缺少：

- 失败自动重试
- 死信队列
- 并发控制
- 任务取消
- 多 Worker 调度策略

### 5.4 媒体分发仍是本地直出思路

目前仍偏本地文件读取与应用层输出，后续如果真要面向部署，应进一步演进为：

- 静态资源网关
- 签名下载地址
- CDN/Nginx 分发

### 5.5 产品化能力还不够丰富

当前已经有分析与可视化反馈，但还缺少更强的用户使用价值模块，例如：

- 成长档案与趋势分析
- 分段训练与高误差片段循环练习
- 教练端批改台
- 报告导出与分享
- 历史对比与里程碑反馈
- 智能训练计划

## 6. 下一阶段建议继续升级的点

建议按“低风险、逐步替换”的节奏继续推进：

### 第一优先级：继续补齐工程底座

1. 为 Redis Worker 增加失败重试和错误分类统计。
2. 为任务增加取消、超时、重跑能力。
3. 为 PostgreSQL 增加更明确的 schema 结构，而不只是存 JSON payload。
4. 为健康检查补充 Redis、PostgreSQL、模型文件、磁盘空间状态。

### 第二优先级：提升接口与存储结构

1. 继续拆分重结果对象，减少单次响应体积。
2. 将报告索引、分析摘要、时间轴摘要结构化入库。
3. 将大文件产物迁移到对象存储或独立媒体目录。

### 第三优先级：增强产品可用性

1. 增加学员成长档案页。
2. 增加分段练习模式。
3. 增加教练端批改页面。
4. 增加报告导出 PDF/分享链接。

## 7. 总结

今天这轮升级，最大的变化不是“多了几个脚本”，而是项目的组织方式发生了变化：

- 从“算法跑完即可”升级为“任务、结果、配置、存储有边界”。
- 从“代码里直接起线程”升级为“执行器与业务逻辑解耦”。
- 从“本地文件堆产物”升级为“运行数据外置、体积可控”。
- 从“演示型接口”升级为“更适合迭代和上线的接口结构”。

用一句话概括：

**升级前的 `dance-assist` 更像一个完整的毕业设计 Demo；升级后的 `dance-assist` 已经开始具备小型工程系统的底座。**

## 8. 相关文件（本次升级核心改动点）

- `backend/app/settings.py`
- `backend/app/main.py`
- `backend/app/api/pipelines.py`
- `backend/app/services/pipeline.py`
- `backend/app/services/storage.py`
- `backend/app/services/task_store.py`
- `backend/app/services/pipeline_executor.py`
- `backend/app/workers/pipeline_redis_worker.py`
- `frontend/src/pages/ComparePage.vue`
- `frontend/src/api/pipelines.ts`
- `frontend/src/types/video.ts`
- `desktop_launcher.py`
- `run-desktop.bat`
- `scripts/start-local-postgres.ps1`
- `scripts/show-local-postgres-status.ps1`
- `scripts/run-redis-pipeline-worker.ps1`
- `scripts/start-redis-pipeline-worker.ps1`
- `scripts/stop-redis-pipeline-worker.ps1`
- `scripts/show-redis-pipeline-worker-status.ps1`

## 9. 后续补充升级（本次继续推进）

### 9.1 健康检查增强

本次继续补齐了昨天计划中的第一个低风险工程化点：**健康检查增强**。

已完成内容：

- 新增健康检查服务：`backend/app/services/health.py`
- 保留原有 `/health` 简洁返回，保证兼容性
- 新增 `/health/ready` 详细健康检查接口
- 检查项已覆盖：
  - 运行目录可写性
  - 运行目录磁盘空间
  - MediaPipe 模型文件是否存在
  - PostgreSQL 连通性与当前数据库信息
  - Redis 连通性与队列积压数量
- 新增配置项：`DANCE_ASSIST_MIN_FREE_DISK_GB`

当前验证结果：

- `/health` 返回：
  - `ok = true`
  - `task_store = postgresql`
  - `pipeline_executor = redis_queue`
- `/health/ready` 返回详细依赖状态，并已验证当前环境全部通过：
  - PostgreSQL：通过
  - Redis：通过
  - 模型文件：通过
  - 磁盘空间：通过
  - 运行目录写入：通过

直接收益：

- 升级前：只能知道服务进没起来，无法知道具体是哪一层依赖出了问题。
- 升级后：可以区分是数据库异常、Redis 不通、模型缺失，还是磁盘空间不足。
- 对桌面版交付、本地排障、后续上线前巡检都更友好。

### 9.2 Redis Worker 失败重试与错误分类统计

本次继续把 Redis 队列这一层补到了更接近生产可用的状态。

已完成内容：

- 为 Redis Worker 增加失败重试机制
- 新增可配置参数：
  - `DANCE_ASSIST_PIPELINE_MAX_RETRIES`
  - `DANCE_ASSIST_PIPELINE_RETRY_BACKOFF_SEC`
- 失败时会根据当前尝试次数决定：
  - 是否重新入队
  - 是否标记为重试耗尽
- 任务状态中补充：
  - `attempt_count`
  - `retry_count`
- 新增失败统计查询能力：
  - 服务层：`get_pipeline_failure_stats()`
  - 接口：`GET /api/pipelines/stats/failures`

本次验证结果：

- 模拟失败任务时：第一次失败后会重新入队，`attempt_count` 从 1 变为 2
- 达到最大尝试次数后：不会继续入队
- 失败统计接口已能正常返回结构化结果
- 当前库中暂无真实失败任务，因此统计结果为空，这属于正常现象

直接收益：

- 升级前：Redis Worker 只要失败一次，就只能靠人工重跑
- 升级后：临时性失败可以自动重试，系统容错性更好
- 同时也为后续做任务看板、异常告警、失败归因分析打下了基础

### 9.3 本地依赖自动启动与便携配置补齐

本次继续补了一轮“开发机和下一台电脑更容易跑起来”的启动能力，重点不是新增算法，而是减少手工环境操作。

已完成内容：

- Redis 启动脚本改为优先读取 `.env` 中的 `DANCE_ASSIST_REDIS_SERVER_EXE`
- 默认兼容当前本机 Redis 安装路径：`D:\Tool\Redis-x64-3.2.100\redis-server.exe`
- `scripts/restart-backend.ps1` 现在会按 `.env` 配置自动确保以下依赖启动：
  - 本地 PostgreSQL
  - 本地 Redis
  - Redis pipeline worker
- `desktop_launcher.py` 已补充本地 Redis 自动拉起逻辑
- `.env.example` 已补充 Redis 可执行文件路径配置说明

直接收益：

- 升级前：开发时经常需要先手动点开 Redis，忘记启动就会导致 `redis_queue` 链路不可用
- 升级后：只要 `.env` 配好，本地开发入口会主动拉起需要的依赖
- Redis 可执行文件位置与项目代码解耦，不需要把 Redis 二进制挪进仓库
- 为后续做“换电脑后尽量少配置、尽量一键启动”打下了基础

当前建议方向：

- Windows 开发机：继续采用 `.env` + 本地依赖自动拉起模式
- 跨平台/新电脑迁移：后续建议进一步整理为统一的 Python bootstrap 脚本，或补一套 Docker Compose 方案
- 如果未来希望做到真正“拿到项目就跑”，还需要继续把 PostgreSQL、Redis、Node、Python 依赖检查统一收口
