# 中期答辩可能提问与回答口径

这份文档按“老师不一定熟悉技术细节”的方式准备。回答时不要急着讲代码，先讲这个系统帮谁解决什么问题，再补一句技术路线。

## 一句话介绍项目

本项目是一个基于视频分析的舞蹈学习辅助平台。它让学生上传教师示范视频和自己的练习视频，系统自动做动作对齐、评分、问题片段定位，并给出复盘建议，帮助学生知道“哪里不一致、应该回看哪一段、下一步怎么练”。

## 老师可能会问的问题

### 1. 你的系统主要解决什么问题？

可以这样答：

传统舞蹈学习主要靠老师现场观察，学生课后复盘时往往只能看完整视频，很难准确知道自己错在什么时间点、哪个动作部位。本系统的目标不是替代老师，而是把视频变成可分析、可回看的训练记录，帮助学生提高课后复盘效率。

### 2. 这个系统和普通视频播放器有什么区别？

可以这样答：

普通播放器只能播放视频，学生需要自己靠肉眼对比。本系统会先提取人体姿态关键点，再把教师和学生动作进行时间对齐，最后生成得分、可信度、问题片段和训练建议。也就是说，它不只是“看视频”，而是把视频转成结构化的复盘结果。

### 3. 核心技术路线是什么？

可以这样答：

整体流程是：上传视频 -> 姿态关键点提取 -> 动作时间对齐 -> 动作差异评分 -> 问题片段定位 -> 报告和 AI 助教解释。核心判断来自姿态识别、DTW 对齐和评分指标，AI 只负责把结果解释得更容易理解。

### 4. DTW 是什么？为什么要用它？

可以这样答：

DTW 可以理解成一种“自动拉齐时间”的方法。教师和学生跳同一段舞时，速度不一定完全一样，有的人某个动作快一点，有的人慢一点。DTW 的作用就是在时间轴上找到更合理的对应关系，让系统不是机械地按同一秒比较，而是尽量比较同一个动作阶段。

### 5. 如果两个视频本身不一样长怎么办？

可以这样答：

这正是时间对齐需要解决的问题。系统不会简单要求两个视频长度完全一致，而是根据动作特征寻找相似片段。对于长度差异较大的视频，系统可以对可匹配部分进行分析，但如果学生视频缺少大量动作内容，可信度会下降，结果更适合作为参考。

### 6. AI 助教是不是直接判断动作对错？

可以这样答：

不是。动作误差不是 AI 判断的，而是由姿态估计、动作对齐和评分指标计算出来的。AI 助教只是读取结构化报告，把分数、问题片段和建议转成更自然的训练语言。这样做可以避免把核心判断完全交给大模型。

### 7. 评分结果准不准？

可以这样答：

目前评分定位是辅助复盘，不是专业裁判分数。它能反映动作相似度、节奏一致性和姿态跟踪质量，但会受视频拍摄质量影响。我的目标是让学生知道哪里需要回看，而不是用一个分数完全替代老师评价。

### 8. 可信度为什么重要？

可以这样答：

因为视频分析依赖人体姿态识别。如果光线不好、身体被遮挡、没有全身入镜，系统虽然也能给出结果，但可信度会降低。可信度就是提醒用户：这次分析结果是否值得参考，是否需要重新拍摄更规范的视频。

### 9. 为什么系统主要要求单人、固定机位？

可以这样答：

这是为了保证中期阶段的分析稳定性。单人和固定机位可以减少背景干扰、多人遮挡和镜头运动造成的识别误差。后续可以扩展到更复杂场景，但当前先保证核心闭环可靠。

### 10. 你的创新点在哪里？

可以这样答：

我的创新点不只是做一个评分，而是把“视频上传、动作对齐、评分、问题片段、回放和训练建议”做成一个闭环。学生可以从分析记录中直接看到问题片段，再跳回对应时间点复盘，比只给一个总分更有实际使用价值。

### 11. 为什么要做 issue index？

可以这样答：

issue index 可以理解成“问题片段目录”。分析完成后，系统会把问题片段单独保存成 `issues.json`。这样记录页打开时不需要每次重新解析完整报告，速度更快，也更适合长期保存历史记录。

### 12. 你的测试怎么做的？

可以这样答：

我主要做了功能测试、算法样例测试、性能测试和 issue index 验证。功能测试覆盖上传、发起分析、查询状态、查看记录、问题片段、AI fallback、删除记录和系统状态检查。样例测试使用当前项目中的教师和学生视频，记录了得分、可信度、问题片段数和耗时。

### 13. 如果现场 pipeline 跑得慢怎么办？

可以这样答：

视频分析本身需要姿态提取和视频输出，现场完整跑一遍可能比较耗时。所以答辩演示时我会准备一组已完成记录，先展示完整闭环；如果时间允许，再现场发起短视频分析任务。这样可以避免把演示风险放在等待计算上。

### 14. 目前系统还有哪些不足？

可以这样答：

第一，当前主要面向单人固定机位，复杂背景和多人场景还不稳定。第二，评分结果仍然是辅助复盘，不能替代专业老师。第三，当前测试样例数量有限，后续需要更多舞种、更多学生水平的数据来验证。第四，性能日志还可以细化到姿态提取、对齐评分、报告生成等阶段。

### 15. 后续计划是什么？

可以这样答：

短期先保证演示稳定，完善测试和论文材料。中期会拆分前端记录页和后端服务模块，提升可维护性。长期计划是积累更多训练记录，做学生个人进步趋势、动作薄弱点统计和更细粒度的训练建议。

## 答辩时建议强调的边界

- 本系统是“辅助复盘工具”，不是替代专业教师。
- AI 助教是“解释报告”，不是“直接判分”。
- 评分结果要结合可信度一起看。
- 当前版本先保证单人、固定机位、离线视频场景稳定。
- 中期重点展示完整闭环：素材库 -> 开始分析 -> 分析记录 -> 问题片段 -> 对照回放 -> AI 建议。

## 遇到追问时的简单回答模板

- 如果老师问“为什么这样设计”：先说用户痛点，再说技术方案。
- 如果老师问“准不准”：回答“辅助复盘，不替代老师”，再解释可信度。
- 如果老师问“AI 做什么”：回答“只解释结构化报告，不负责动作误差判断”。
- 如果老师问“有什么不足”：主动承认样例数量、拍摄条件和复杂场景限制。
- 如果老师问“后面怎么做”：回答“补数据、补测试、拆模块、做长期训练档案”。
